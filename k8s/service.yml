apiVersion: v1
kind: Service
metadata:
  name: sokmer
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sokmer"
    port: 8080
  selector:
    app: sokmer
