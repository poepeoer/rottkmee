apiVersion: v1
kind: Service
metadata:
  name: xjsero
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xjsero"
    port: 8080
  selector:
    app: xjsero
