apiVersion: v1
kind: Service
metadata:
  name: huwero
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "huwero"
    port: 8080
  selector:
    app: huwero
